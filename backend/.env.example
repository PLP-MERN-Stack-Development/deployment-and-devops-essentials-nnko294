# Example backend environment variables (do NOT commit real secrets)
# Copy this file to `backend/.env` for local testing and fill values.

NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb+srv://<dbUser>:<password>@cluster0.abcd123.mongodb.net/myDatabase?retryWrites=true&w=majority
SENTRY_DSN=

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret_here

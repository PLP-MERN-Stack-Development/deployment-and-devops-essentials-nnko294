# Example environment variables for production and CI
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb+srv://<dbUser>:<password>@cluster0.example.mongodb.net/myDatabase?retryWrites=true&w=majority
JWT_SECRET=

# Render (backend) - optional
RENDER_SERVICE_ID=
RENDER_API_KEY=

# Netlify (frontend) - optional
NETLIFY_SITE_ID=
NETLIFY_AUTH_TOKEN=

# Other secrets (Sentry, etc.)
SENTRY_DSN=
